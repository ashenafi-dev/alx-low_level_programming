#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 * _strlen_recursion - prints string length.
 * @s: input value.
 *
 * Return: length of a given string.
 */

int _strlen_recursion(char *s)
{
	return (strlen(s));
}
