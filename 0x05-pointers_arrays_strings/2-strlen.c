#include "main.h"
#include <string.h>

/**
 * _strlen  - a function that takes a pointer to an int as
 * parameter and return a length of the string
 * @s: input
 * Return: strlen(*s)
 */

int _strlen(char *s)
{
	return (strlen(s));
}
